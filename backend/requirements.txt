# Core
fastapi
uvicorn[standard]
gunicorn
pydantic
pydantic-settings

# PostgreSQL (async + sync for Alembic)
asyncpg
psycopg2-binary
sqlalchemy[asyncio]
alembic

# One-time SQLite -> Postgres data import (used by entrypoint on first boot only)
aiosqlite

# Redis
redis[hiredis]

# Celery (Phase 3)
celery[redis]

# Existing dependencies carried forward
aiohttp
aiofiles
httpx
pandas
python-dotenv
python-multipart
openai
pyotp
itsdangerous

# Google Sheets write-back (used by legacy get_quote.py to update sheet)
gspread
oauth2client
google-auth
google-auth-oauthlib

# Testing
pytest
pytest-asyncio

# Monitoring (optional)
sentry-sdk[fastapi]

# Load testing (dev only)
locust
